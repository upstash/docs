---
title: "Quickstart"
---

**Upstash Box lets you give your AI agents a computer.**

Every box is a secure, isolated cloud container with an AI agent like Claude Code or Codex built-in. Spin up as many as you want in parallel. Each one includes a full environment with a filesystem, git, and a runtime. Your agent can read files, write code, and execute tasks inside it.

---

### 1. Get your API key

Navigate to the [Upstash Console](https://console.upstash.com) and create a new Box API key.

<Frame>
  <img src="/img/box/create-api-key.png" />
</Frame>

### 2. Install the SDK

<CodeGroup>
```bash npm
npm install @upstash/box
```

```bash yarn
yarn add @upstash/box
```

```bash pnpm
pnpm add @upstash/box
```

```bash bun
bun install @upstash/box
```

</CodeGroup>

### 3. Set Environment Variables

Add your Upstash Box API key:

```bash title=".env"
UPSTASH_BOX_API_KEY=abx_xxxxxxxxxxxxxxxxxxxxxxxx
```

### 4. Create a Box

```typescript title="lib/box.ts"
import { Box } from "@upstash/box"

const box = await Box.create({
  runtime: "node",
})
```

Your box is ready to use! You can already use it as a standalone, secure, isolated sandbox with full shell access, git, and filesystem operations.

---

### 5. Configure an Agent (optional)

To configure an agent for your box, pass the model you want to use and the provider API key:

<Tabs>
<Tab title="Claude">
```typescript {5-8}
import { Box, ClaudeCode } from "@upstash/box"

const box = await Box.create({
  runtime: "node",
  agent: {
    model: ClaudeCode.Sonnet_4_5,
    apiKey: process.env.ANTHROPIC_API_KEY,
  },
})
```
</Tab>

<Tab title="Codex">
```typescript {5-8}
import { Box, OpenAICodex } from "@upstash/box"

const box = await Box.create({
  runtime: "node",
  agent: {
    model: OpenAICodex.GPT_5_3_Codex,
    apiKey: process.env.OPENAI_API_KEY,
  },
})
```
</Tab>
</Tabs>

### 6. Run Your First Task

Your box is ready to use! It's a secure cloud environment to run agents, execute commands, or manage files. For example:

```typescript
import { Box, ClaudeCode } from "@upstash/box"

const box = await Box.create({
  runtime: "node",
  agent: {
    model: ClaudeCode.Sonnet_4_5,
    apiKey: process.env.ANTHROPIC_API_KEY,
  },
})

// 👇 execute OS-level commands
await box.exec("node --version")

// 👇 run agent
await box.agent.run({
  prompt: "create an index.txt saying 'hello world'",
})
```

---

## Next Steps

<CardGroup cols={2}>
  <Card title="How a box works" href="/box/overall/how-it-works">
    Full TypeScript SDK reference and examples
  </Card>

  <Card title="Agent" href="/box/overall/agent">
    Agent
  </Card>
</CardGroup>
