---
title: "DELEX"
description: "Delete a string when a condition matches."
---

Use `DELEX` to delete a string when a condition matches.

## Syntax

```redis
DELEX <key> [IFEQ <value> | IFNE <value> | IFDEQ <digest> | IFDNE <digest>]
```

## Arguments

| Argument | Required | Repeatable | Description |
| --- | --- | --- | --- |
| `key` | Yes | No | String key to delete. |
| `IFEQ value \| IFNE value` | No | No | Delete when the stored string is equal to or different from the supplied value. |
| `IFDEQ digest \| IFDNE digest` | No | No | Delete when the stored string digest is equal to or different from the supplied 16-character digest. |

## Important points

- Supply at most one condition. With no condition, `DELEX` behaves like deleting a single key.
- Conditional forms accept string keys only. A non-string value returns an error and is not deleted.

## Response

The reply reports the result of the operation. Error replies have the same shape in RESP2 and RESP3 and are surfaced as exceptions by the SDKs below.

| Protocol | Reply |
| --- | --- |
| RESP2 | Integer |
| RESP3 | Integer |

<Note>
  Client libraries often decode bulk strings, maps, sets, and numeric strings into language-native values. The table describes the Redis wire reply.
</Note>

## Examples

TCP examples use the TLS `REDIS_URL` from the Upstash console. REST examples use `UPSTASH_REDIS_REST_URL` and `UPSTASH_REDIS_REST_TOKEN`.

<Tabs>

<Tab title="@upstash/redis">

<Note>
  This command is not supported yet in `@upstash/redis`.
</Note>

</Tab>

<Tab title="upstash_redis">

<Note>
  This command is not supported yet in `upstash_redis`.
</Note>

</Tab>

<Tab title="ioredis">

```ts
import Redis from "ioredis";

const redis = new Redis(process.env.REDIS_URL!);
const result = await redis.call("DELEX", "lock:job-1", "IFEQ", "worker-1");
console.log(result);
```

</Tab>

<Tab title="node-redis">

```ts
import { createClient } from "redis";

const client = await createClient({ url: process.env.REDIS_URL })
  .on("error", console.error)
  .connect();
const result = await client.delEx("lock:job-1", { condition: "IFEQ", matchValue: "worker-1" });
console.log(result);
```

</Tab>

<Tab title="redis-py">

```python
import os
import redis

client = redis.from_url(os.environ["REDIS_URL"])
result = client.execute_command("DELEX", "lock:job-1", "IFEQ", "worker-1")
print(result)
```

</Tab>

<Tab title="go-redis">

```go
package main

import (
    "context"
    "fmt"
    "os"

    "github.com/redis/go-redis/v9"
)

func main() {
    opts, err := redis.ParseURL(os.Getenv("REDIS_URL"))
    if err != nil {
        panic(err)
    }
    client := redis.NewClient(opts)
    result, err := client.DelExArgs(context.Background(), "lock:job-1", redis.DelExArgs{Mode: "IFEQ", MatchValue: "worker-1"}).Result()
    if err != nil {
        panic(err)
    }
    fmt.Println(result)
}
```

</Tab>

<Tab title="jedis">

```java
import java.net.URI;

import redis.clients.jedis.Jedis;

try (Jedis jedis = new Jedis(new URI(System.getenv("REDIS_URL")))) {
  Object result = jedis.delex("lock:job-1", redis.clients.jedis.util.CompareCondition.valueEq("worker-1"));
  System.out.println(result);
}
```

</Tab>

<Tab title="redis-rs">

```rust
use redis::TypedCommands;

fn main() -> redis::RedisResult<()> {
    let url = std::env::var("REDIS_URL").expect("REDIS_URL is not set");
    let client = redis::Client::open(url)?;
    let mut connection = client.get_connection()?;

    let result = connection.del_ex("lock:job-1", redis::ValueComparison::ifeq("worker-1"))?;
    println!("{result:?}");
    Ok(())
}
```

</Tab>

</Tabs>
