---
title: Reset
---

The `reset` method allows you to clear all documents from a particular index.

## Response

`'Success'` if the collection is successfully reset.

<RequestExample>

```typescript Basic
const responseReset = await index.reset();
// 'Success'
```

</RequestExample>
