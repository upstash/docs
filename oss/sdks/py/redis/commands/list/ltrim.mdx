---
title: LTRIM
description: Trim a list to the specified range
---

## Arguments

<ParamField body="key" type="str" required>
  The key of the list.
</ParamField>

<ParamField body="start" type="number" required>
  The start index of the range to trim.
</ParamField>


<ParamField body="end" type="TValue" required>
  The end index of the range to trim.
</ParamField>

## Response

<ResponseField type="OK" required>
  `OK`
</ResponseField>

<RequestExample>
```py Example 
redis.lpush("key", "a", "b", "c", "d"); 
redis.ltrim("key", 1, 2); 
# the list is now ["b", "c"]
 ```
</RequestExample>
