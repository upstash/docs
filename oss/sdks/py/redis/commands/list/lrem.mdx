---
title: LREM
description: Remove the first `count` occurences of an element from a list.
---

## Arguments

<ParamField body="key" type="string" required>
  The key of the list.
</ParamField>

<ParamField body="count" type="number" required>
 How many elements to remove.
</ParamField>


<ParamField body="element" type="TValue" required>
 The element to remove
</ParamField>

## Response

<ResponseField type="integer" required>
  The number of elements removed.
</ResponseField>

<RequestExample>
```py Example 
redis.lpush("key", "a", "a", "b", "b", "c"); 
removed = redis.lrem("key", 4, "b"); 
print(removed) # 2
 ```
</RequestExample>
