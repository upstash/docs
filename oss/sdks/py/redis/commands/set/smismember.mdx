---
title: SMISMEMBER
description: Check if multiple members exist in a set
---

## Arguments

<ParamField body="key" type="string" required>
  The key of the set to check.
</ParamField>

<ParamField body="members" type="TMember[]">
  The members to check
</ParamField>


## Response

<ResponseField type="(0 | 1)[]" required>
  An array of `0` and `1` values.
  `1` if the member exists in the set, `0` if not.
</ResponseField>

<RequestExample>
```py Example 
await redis.sadd("set", "a", "b", "c"); 
members =  await redis.smismember("set", ["a", "b", "d"])
print(members); # [1, 1, 0]
```

</RequestExample>
