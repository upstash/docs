---
title: URL Groups
---

<Info>
  You can run the async code by importing `AsyncQStash` from `upstash_qstash`
  and awaiting the methods.
</Info>

#### Create a URL group and add 2 endpoints

```python
from upstash_qstash import QStash

qstash = QStash("<QSTASH-TOKEN>")
qstash.url_group.upsert_endpoints(
    url_group="my-url-group",
    endpoints=[
        {"url": "https://my-endpoint-1"},
        {"url": "https://my-endpoint-2"},
    ],
)
```

#### Get URL group by name

```python
from upstash_qstash import QStash

qstash = QStash("<QSTASH-TOKEN>")
url_group = qstash.url_group.get("my-url-group")

print(url_group.name, url_group.endpoints)
```

#### List URL groups

```python
from upstash_qstash import QStash

qstash = QStash("<QSTASH-TOKEN>")
all_url_groups = qstash.url_group.list()

for url_group in all_url_groups:
    print(url_group.name, url_group.endpoints)
```

#### Remove an endpoint from a URL group

```python
from upstash_qstash import QStash

qstash = QStash("<QSTASH-TOKEN>")
qstash.url_group.remove_endpoints(
    url_group="my-url-group",
    endpoints=[
        {"url": "https://my-endpoint-1"},
    ],
)
```

#### Delete a URL group

```python
from upstash_qstash import QStash

qstash = QStash("<QSTASH-TOKEN>")
qstash.url_group.delete("my-url-group")
```
