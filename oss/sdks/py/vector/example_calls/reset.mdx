---
title: Reset
---

## Method

The `reset` method allows you to clear all vectors and metadata from the index.

## Reset Example

```python
from upstash_vector import Index
index = Index.from_env()

index.reset()
```

It's also possible to specify a namespace when resetting the index:

```python
index.reset("ns0")
```
